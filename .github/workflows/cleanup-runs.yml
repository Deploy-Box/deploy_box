name: Cleanup old workflow runs

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  cleanup:
    uses: Deploy-Box/deploy-box-platform/.github/workflows/cleanup-runs.yml@793663640997f5ed1f18553ec682deb5c01126fd
