name: Cleanup old workflow runs

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  cleanup:
    uses: Deploy-Box/deploy-box-platform/.github/workflows/cleanup-runs.yml@dev
    permissions:
      actions: write
      contents: read
